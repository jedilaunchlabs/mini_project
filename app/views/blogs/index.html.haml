%br/
= button_to 'Create Blog', new_blog_path, :method => "get"

- unless @blogs.empty?
  %h2 Blogs

  %table
    %thead
      %tr
        %th Title
        %th Caption
        %th Description
        %th User
        %th{:colspan => "4"}
    %tbody
      - @blogs.each do |blog|
        %tr
          %td= blog.title
          %td= blog.caption
          %td= blog.description
          %td= blog.user.email
          %td= button_to 'Delete', blog, :method => "delete", data: { confirm: 'Are you sure?' }

          - if blog.is_draft?
            %td= button_to 'Edit', edit_blog_path(blog), :method => "get"
            %td= button_to 'Live'

          - unless blog.is_draft?
            %td= button_to 'Archive'
  %br/        
  = will_paginate
- else
  %p Blog list is empty.

